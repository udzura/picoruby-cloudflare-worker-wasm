diff --git a/src/re_exec.c b/src/re_exec.c
--- a/src/re_exec.c
+++ b/src/re_exec.c
@@ -720,6 +720,11 @@ bt_match(const mrb_regexp_pattern *pat, const char *str, const char *str_end,
       pc++;
       break;
-
+
+    case RE_EOTNL:
+      if (sp != str_end && !(sp + 1 == str_end && *sp == '\n')) return FALSE;
+      pc++;
+      break;
+
     case RE_WBOUND:
       {
