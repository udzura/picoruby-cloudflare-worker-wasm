{
  "name": "picoruby-cloudflare-worker-spike",
  "private": true,
  "type": "module",
  "scripts": {
    "sync:templates": "rake -f Rakefile templates",
    "generate:bindings": "node scripts/generate-bindings.mjs",
    "check:bindings": "node scripts/generate-bindings.mjs --check",
    "build": "npm run sync:templates && npm run generate:bindings && rake -f Rakefile build",
    "build:app": "rake -f Rakefile app",
    "clean": "rake -f Rakefile clean",
    "watch:app": "node scripts/watch-app.mjs",
    "dev": "node scripts/watch-app.mjs --with-wrangler",
    "check": "WRANGLER_LOG_PATH=.wrangler/wrangler.log wrangler deploy --dry-run --outdir .wrangler-dist",
    "test": "npm run check:bindings && node test/binding-registry.mjs && node test/durable-object.mjs && node test/durable-object-workerd.mjs && node test/d1.mjs && node test/d1-workerd.mjs && node test/fetch.mjs && node test/fetch-workerd.mjs && node test/runtime.mjs"
  },
  "devDependencies": {
    "jsonc-parser": "3.2.0",
    "wrangler": "4.125.0"
  }
}
